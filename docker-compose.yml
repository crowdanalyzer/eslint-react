version: '3'
services:
  node:
    tty: true
    stdin_open: true
    container_name: eslint-react
    build:
      context: .
      args:
        - NPM_TOKEN
    volumes:
      - .:/var/code:rw
    entrypoint: './startup.sh'
